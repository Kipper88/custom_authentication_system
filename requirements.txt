# Core runtime uses the Python standard library.
# FastAPI is included for the optional first-class integration module:
# custom_auth.fastapi.CustomAuthMiddleware.
fastapi>=0.115,<1.0
uvicorn>=0.34,<1.0
